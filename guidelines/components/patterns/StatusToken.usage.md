# StatusToken

```jsx
<StatusToken label="PENDING" />
<StatusToken label="CASH WITHDRAWAL" />
```

10px uppercase, weight 500, no fill — the coloured word IS the token. Used above transaction
amounts and as the Filter drawer's checkbox labels; never restyle it per table.
