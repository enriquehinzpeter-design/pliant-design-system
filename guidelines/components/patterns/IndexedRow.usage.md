# IndexedRow

```jsx
<IndexedRow index={1} title="CRP Sanity Template (harness 20260720)" subtitle="Template"
  leading={<CardIcon colourway="gray" scheme="visa" />}
  trailing={<StatusBadge label="ACTIVE" color="success" />} />
```

Ordered configuration lists where position matters (card templates). Rows are separate
outlined cards, 22px apart — not a hairline table.
