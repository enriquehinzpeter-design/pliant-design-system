The tiny uppercase role tag set inline after a member's name — OWNER on the internal Members list (INT-02).

```jsx
<span>Aaron Aaijtink<RoleTag label="Owner" /></span>
```

It is **typographic, not a pill**: no fill, no border, raised slightly off the name's baseline. Only OWNER has been observed; other roles reuse it as they appear.
