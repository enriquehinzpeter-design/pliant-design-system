# FormControl

**Group:** Forms · **Kind:** MUI re-export (`@mui/material/FormControl`)

> Synced from `infinnity-frontend` `packages/ui/src/components/FormControl/index.tsx`.

## Import

```tsx
import { FormControl } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/FormControl`. Props are MUI's `FormControlProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

No Storybook story. Usage is not captured here — read the component source, or the call sites in `infinnity-frontend`, before assuming behaviour.
