Multi-select and boolean opt-in. Checked state is near-black (`--primary-main`), never a colour.

```jsx
<Checkbox label="Require receipt for every transaction" defaultChecked />
<Checkbox indeterminate size="small" />
```

Use `size="small"` inside data grids — that is the exact 20px glyph / 2px padding the small grid override applies.
