# Slider

**Group:** Forms · **Kind:** MUI re-export (`@mui/material/Slider`)

> Synced from `infinnity-frontend` `packages/ui/src/components/Slider/index.tsx`.

## Import

```tsx
import { Slider } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/Slider`. Props are MUI's `SliderProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/Slider/index.stories.tsx` (9 stories):

`Default` · `Small` · `Disabled` · `WithMarks` · `Range` · `ValueLabel` · `Vertical` · `WithFormControl` · `WithTypographyValue`
