# RadioGroup

**Group:** Forms · **Kind:** Pliant wrapper around `@mui/material/RadioGroup`

> Synced from `infinnity-frontend` `packages/ui/src/components/RadioGroup/index.tsx`.

## Import

```tsx
import { RadioGroup } from '@pliant/ui';
```

## API

Exports `RadioGroup`, with types `RadioGroupProps`.

Requires `aria-label` or `aria-labelledby` — a radio group has no visible label of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

No Storybook story. Usage is not captured here — read the component source, or the call sites in `infinnity-frontend`, before assuming behaviour.
