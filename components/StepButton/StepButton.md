# StepButton

**Group:** Navigation · **Kind:** MUI re-export (`@mui/material/StepButton`)

> Synced from `infinnity-frontend` `packages/ui/src/components/StepButton/index.tsx`.

## Import

```tsx
import { StepButton } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/StepButton`. Props are MUI's `StepButtonProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

No Storybook story. Usage is not captured here — read the component source, or the call sites in `infinnity-frontend`, before assuming behaviour.
