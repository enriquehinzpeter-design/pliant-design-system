# Card

**Group:** Core · **Kind:** MUI re-export (`@mui/material/Card`)

> Synced from `infinnity-frontend` `packages/ui/src/components/Card/index.tsx`.

## Import

```tsx
import { Card } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/Card`. Props are MUI's `CardProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/Card/index.stories.tsx` (5 stories):

`Default` · `ElevationsOverview` · `Outlined` · `WithMedia` · `WithFooterActions`
