# Fade

**Group:** Transitions · **Kind:** MUI re-export (`@mui/material/Fade`)

> Synced from `infinnity-frontend` `packages/ui/src/components/Fade/index.tsx`.

## Import

```tsx
import { Fade } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/Fade`. Props are MUI's `FadeProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/Fade/index.stories.tsx` (1 story):

`Default`
