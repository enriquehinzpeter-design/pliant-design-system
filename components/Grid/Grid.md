# Grid

**Group:** Layout · **Kind:** MUI re-export (`@mui/material/Grid`)

> Synced from `infinnity-frontend` `packages/ui/src/components/Grid/index.tsx`.

## Import

```tsx
import { Grid } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/Grid`. Props are MUI's `GridProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/Grid/index.stories.tsx` (5 stories):

`Responsive` · `Spacing` · `Alignment` · `Nested` · `Fluid`
