# Box

**Group:** Layout · **Kind:** MUI re-export (`@mui/material/Box`)

> Synced from `infinnity-frontend` `packages/ui/src/components/Box/index.tsx`.

MUI's styling primitive. The house layout tool — reach for it before writing a styled component.

## Import

```tsx
import { Box } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/Box`. Props are MUI's `BoxProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/Box/index.stories.tsx` (2 stories):

`Default` · `FlexRow`
