# Avatar

**Group:** Core · **Kind:** MUI re-export (`@mui/material/Avatar`)

> Synced from `infinnity-frontend` `packages/ui/src/components/Avatar/index.tsx`.

## Import

```tsx
import { Avatar } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/Avatar`. Props are MUI's `AvatarProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/Avatar/index.stories.tsx` (7 stories):

`Default` · `Rounded` · `Square` · `WithImage` · `WithIcon` · `WithBadgeDot` · `WithBadgeCount`
