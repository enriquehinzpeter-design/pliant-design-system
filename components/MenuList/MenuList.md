# MenuList

**Group:** Overlays · **Kind:** MUI re-export (`@mui/material/MenuList`)

> Synced from `infinnity-frontend` `packages/ui/src/components/MenuList/index.tsx`.

## Import

```tsx
import { MenuList } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/MenuList`. Props are MUI's `MenuListProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

No Storybook story. Usage is not captured here — read the component source, or the call sites in `infinnity-frontend`, before assuming behaviour.
