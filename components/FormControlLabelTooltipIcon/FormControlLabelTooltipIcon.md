# FormControlLabelTooltipIcon

**Group:** Forms · **Kind:** Pliant component (no MUI base)

> Synced from `infinnity-frontend` `packages/ui/src/components/FormControlLabelTooltipIcon/index.tsx`.

16px info icon in a tooltip, tinted `text.secondary`, for explaining a checkbox/switch row label. Takes Tooltip's props minus `children`.

## Import

```tsx
import { FormControlLabelTooltipIcon } from '@pliant/ui';
```

## API

Exports `FormControlLabelTooltipIcon`, with types `FormControlLabelTooltipIconProps`.

## Pliant restyling

No override module — this component is not a MUI component, so it reads tokens directly in its own source (see the source path above).

## Usage

No Storybook story. Usage is not captured here — read the component source, or the call sites in `infinnity-frontend`, before assuming behaviour.

Pass the explanation as `title`. Sits inline after the label text.
