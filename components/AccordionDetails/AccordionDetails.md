# AccordionDetails

**Group:** Navigation · **Kind:** MUI re-export (`@mui/material/AccordionDetails`)

> Synced from `infinnity-frontend` `packages/ui/src/components/AccordionDetails/index.tsx`.

Detail body of an Accordion. Its 32px bottom padding is set from the Accordion override, not here.

## Import

```tsx
import { AccordionDetails } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/AccordionDetails`. Props are MUI's `AccordionDetailsProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

No Storybook story. Usage is not captured here — read the component source, or the call sites in `infinnity-frontend`, before assuming behaviour.
