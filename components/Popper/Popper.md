# Popper

**Group:** Overlays · **Kind:** MUI re-export (`@mui/material/Popper`)

> Synced from `infinnity-frontend` `packages/ui/src/components/Popper/index.tsx`.

## Import

```tsx
import { Popper } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/Popper`. Props are MUI's `PopperProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/Popper/index.stories.tsx` (2 stories):

`Default` · `PlacementRightStart`
