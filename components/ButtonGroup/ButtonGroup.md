# ButtonGroup

**Group:** Core · **Kind:** MUI re-export (`@mui/material/ButtonGroup`)

> Synced from `infinnity-frontend` `packages/ui/src/components/ButtonGroup/index.tsx`.

## Import

```tsx
import { ButtonGroup } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/ButtonGroup`. Props are MUI's `ButtonGroupProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/ButtonGroup/index.stories.tsx` (7 stories):

`Default` · `Outlined` · `Text` · `Vertical` · `Small` · `FullWidth` · `Disabled`
