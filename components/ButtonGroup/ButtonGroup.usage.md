Groups related Buttons into one segmented control with shared edges.

```jsx
<ButtonGroup>
  <Button>Day</Button><Button>Week</Button><Button>Month</Button>
</ButtonGroup>
```

Only the outer corners are rounded; inner borders collapse via a -1px margin.
