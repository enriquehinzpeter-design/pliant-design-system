# Radio

**Group:** Forms · **Kind:** Pliant wrapper around `@mui/material/Radio`

> Synced from `infinnity-frontend` `packages/ui/src/components/Radio/index.tsx`.

Colour narrowed to `primary` (default) or `default`, like Checkbox and Switch.

## Import

```tsx
import { Radio } from '@pliant/ui';
```

## API

Exports `Radio`, with types `RadioProps`.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/Radio/index.stories.tsx` (6 stories):

`Small` · `Medium` · `WithLabel` · `WithLabelDisabled` · `WithFormControlAndHelperComponents` · `WithFormControlAndHelperComponentsRowError`
