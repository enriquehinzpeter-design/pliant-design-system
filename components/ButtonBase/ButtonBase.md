# ButtonBase

**Group:** Core · **Kind:** MUI re-export (`@mui/material/ButtonBase`)

> Synced from `infinnity-frontend` `packages/ui/src/components/ButtonBase/index.tsx`.

Unstyled interactive surface. Use only when no real button or link fits; it carries none of the Pliant button styling.

## Import

```tsx
import { ButtonBase } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/ButtonBase`. Props are MUI's `ButtonBaseProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/ButtonBase/index.stories.tsx` (2 stories):

`Default` · `Disabled`
