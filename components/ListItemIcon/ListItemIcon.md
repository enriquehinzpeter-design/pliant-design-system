# ListItemIcon

**Group:** Navigation · **Kind:** MUI re-export (`@mui/material/ListItemIcon`)

> Synced from `infinnity-frontend` `packages/ui/src/components/ListItemIcon/index.tsx`.

## Import

```tsx
import { ListItemIcon } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/ListItemIcon`. Props are MUI's `ListItemIconProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

No Storybook story. Usage is not captured here — read the component source, or the call sites in `infinnity-frontend`, before assuming behaviour.
