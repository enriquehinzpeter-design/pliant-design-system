# Collapse

**Group:** Transitions · **Kind:** MUI re-export (`@mui/material/Collapse`)

> Synced from `infinnity-frontend` `packages/ui/src/components/Collapse/index.tsx`.

## Import

```tsx
import { Collapse } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/Collapse`. Props are MUI's `CollapseProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/Collapse/index.stories.tsx` (3 stories):

`Default` · `Horizontal` · `PartialHeightWhenCollapsed`
