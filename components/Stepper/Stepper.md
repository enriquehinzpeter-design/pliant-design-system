# Stepper

**Group:** Navigation · **Kind:** MUI re-export (`@mui/material/Stepper`)

> Synced from `infinnity-frontend` `packages/ui/src/components/Stepper/index.tsx`.

## Import

```tsx
import { Stepper } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/Stepper`. Props are MUI's `StepperProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/Stepper/index.stories.tsx` (7 stories):

`Horizontal` · `Interactive` · `Vertical` · `NonLinear` · `WithOptionalLabel` · `WithError` · `WithStepIconAndConnector`
