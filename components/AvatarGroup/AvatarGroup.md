# AvatarGroup

**Group:** Core · **Kind:** MUI re-export (`@mui/material/AvatarGroup`)

> Synced from `infinnity-frontend` `packages/ui/src/components/AvatarGroup/index.tsx`.

## Import

```tsx
import { AvatarGroup } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/AvatarGroup`. Props are MUI's `AvatarGroupProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/AvatarGroup/index.stories.tsx` (4 stories):

`Default` · `MaxTwo` · `SpacingSmall` · `CustomSurplusWithTotal`
