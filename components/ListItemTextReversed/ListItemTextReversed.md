# ListItemTextReversed

**Group:** Navigation · **Kind:** Pliant component (no MUI base)

> Synced from `infinnity-frontend` `packages/ui/src/components/ListItemTextReversed/index.tsx`.

`ListItemText` with the rows flipped (`column-reverse`) so the secondary line reads above the primary one — for label-over-value rows.

> Use in place of the ListItemText component if you need
> to display the secondary text before the primary one.

## Import

```tsx
import { ListItemTextReversed } from '@pliant/ui';
```

## API

Exports `ListItemTextReversed`.

## Pliant restyling

No override module — this component is not a MUI component, so it reads tokens directly in its own source (see the source path above).

## Usage

No Storybook story. Usage is not captured here — read the component source, or the call sites in `infinnity-frontend`, before assuming behaviour.
