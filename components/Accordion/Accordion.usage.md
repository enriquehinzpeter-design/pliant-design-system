Progressive disclosure for secondary detail — advanced card settings, per-integration field mapping, FAQ blocks.

```jsx
<Accordion title="Advanced limits" defaultExpanded>…</Accordion>
<Accordion title="Merchant restrictions">…</Accordion>
```

Pliant's override strips elevation and gutters: a plain bottom `--divider` hairline, no rounded last child, and 32px bottom padding on the expanded body.
