# StepIcon

**Group:** Navigation · **Kind:** MUI re-export (`@mui/material/StepIcon`)

> Synced from `infinnity-frontend` `packages/ui/src/components/StepIcon/index.tsx`.

## Import

```tsx
import { StepIcon } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/StepIcon`. Props are MUI's `StepIconProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

No Storybook story. Usage is not captured here — read the component source, or the call sites in `infinnity-frontend`, before assuming behaviour.
