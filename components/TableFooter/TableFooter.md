# TableFooter

**Group:** Data · **Kind:** MUI re-export (`@mui/material/TableFooter`)

> Synced from `infinnity-frontend` `packages/ui/src/components/TableFooter/index.tsx`.

## Import

```tsx
import { TableFooter } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/TableFooter`. Props are MUI's `TableFooterProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

No Storybook story. Usage is not captured here — read the component source, or the call sites in `infinnity-frontend`, before assuming behaviour.
