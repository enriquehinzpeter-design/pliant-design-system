# Divider

**Group:** Core · **Kind:** MUI re-export (`@mui/material/Divider`)

> Synced from `infinnity-frontend` `packages/ui/src/components/Divider/index.tsx`.

The house separator. Pliant favours borders and dividers over shadows (docs/01).

## Import

```tsx
import { Divider } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/Divider`. Props are MUI's `DividerProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/Divider/index.stories.tsx` (3 stories):

`Horizontal` · `Vertical` · `WithText`
