# List

**Group:** Navigation · **Kind:** MUI re-export (`@mui/material/List`)

> Synced from `infinnity-frontend` `packages/ui/src/components/List/index.tsx`.

## Import

```tsx
import { List } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/List`. Props are MUI's `ListProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/List/index.stories.tsx` (6 stories):

`Default` · `Dense` · `Navigation` · `WithSubheadersAndDividers` · `WithAvatarTextAndTrash` · `WithListItemTextReversed`
