Shows where a detail view sits — used above card, employee and transaction detail headers.

```jsx
<Breadcrumbs items={[{ label: 'Cards', href: '/cards' }, { label: '•• 4821' }]} />
```

The last crumb is plain `text.primary` and not a link.
