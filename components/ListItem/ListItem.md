# ListItem

**Group:** Navigation · **Kind:** MUI re-export (`@mui/material/ListItem`)

> Synced from `infinnity-frontend` `packages/ui/src/components/ListItem/index.tsx`.

## Import

```tsx
import { ListItem } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/ListItem`. Props are MUI's `ListItemProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/ListItem/index.stories.tsx` (5 stories):

`PrimarySecondary` · `PrimarySecondaryReversed` · `DisableGutters` · `Dense` · `WithIconSecondaryActionAndDivider`
