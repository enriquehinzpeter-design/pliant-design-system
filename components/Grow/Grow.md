# Grow

**Group:** Transitions · **Kind:** MUI re-export (`@mui/material/Grow`)

> Synced from `infinnity-frontend` `packages/ui/src/components/Grow/index.tsx`.

## Import

```tsx
import { Grow } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/Grow`. Props are MUI's `GrowProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/Grow/index.stories.tsx` (1 story):

`Default`
