# Stack

**Group:** Layout · **Kind:** MUI re-export (`@mui/material/Stack`)

> Synced from `infinnity-frontend` `packages/ui/src/components/Stack/index.tsx`.

## Import

```tsx
import { Stack } from '@pliant/ui';
```

## API

Re-exported from `@mui/material/Stack`. Props are MUI's `StackProps` — this library adds no props of its own.

## Pliant restyling

None. This component inherits the theme (palette, typography, `shape.borderRadius`) but has no component-level override module, so its MUI defaults stand.

## Usage

Canonical usage is the Storybook story set — `packages/ui/src/components/Stack/index.stories.tsx` (5 stories):

`Column` · `Row` · `ResponsiveDirection` · `Alignment` · `FlexWrap`
